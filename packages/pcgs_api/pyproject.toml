[project]
name = "pcgs-api"
version = "0.1.0"
description = "Python wrapper for the Public PCGS API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0",
    "pydantic>=2.11.7",
]
